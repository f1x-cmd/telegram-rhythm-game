# Задачи

Стабильные ID (`T-12`) — на них ссылаются коммиты и другие файлы.

## IN PROGRESS

- **T-01** · Довести ощущение от игры — главный приоритет владельца.
  Сделано: доводка стрелки направления и наказания за разрез поперёк, голос
  промахов, длительность пыли от бомбы, цвет подписей по смыслу, шрифт попаданий,
  пауза перестала съедать свайпы, жест «назад» убран из игровой зоны.
  Осталось: списка претензий нет — его нужно собрать. Готово, когда партия
  от начала до конца ощущается как одно движение, без «игра меня не слышит».

## NEXT

- **T-02** · Свежий взгляд: сыграть несколько партий в обоих режимах на всех
  сложностях и **самому предложить владельцу 3–5 главных проблем** с оценкой,
  что даст больше всего. Владелец прямо просит инициативу (`D-19`).
  Начинать с игры, а не с чтения кода.
- **T-03** · Минимальная страховка от регрессий. Тестов нет вообще (`T-05`),
  поэтому любая правка баланса или ввода рискует сломать соседнее.
  Хотя бы: чек-лист ручной проверки партии, который агент проходит перед push.
- **T-04** · Подключить донат Telegram Stars: заполнить `DONATE.invoices`
  (или `url` / `bot`) в `js/config.js`. Сейчас кнопки пакетов не ведут к оплате.
  Требует действий владельца в BotFather — сам агент это не сделает.

## BLOCKED

- **T-04** заблокирована владельцем: ссылки `createInvoiceLink` создаются в
  Telegram, у агента нет доступа к боту.

## IDEAS

Не обязательства.

- **T-05** · Автотесты чистых функций (`analysis.js`, `records.js`, `career.js`,
  `daily.js`, `api/_lib/guard.js`) — считают, не рисуют, легко проверяются.
  Цена: появится `package.json` и шаг сборки, что спорит с `D-01`. Обсудить.
- **T-06** · Сделать LiveOps серверным: сейчас панель правит только своё
  устройство, перенос настроек — выгрузкой JSON руками.
- **T-07** · Привлечение игроков (приглашения, вызовы, шаринг карточек).
  Владелец сознательно отложил: сейчас важнее ощущение, чем охват.
- **Отвергнуто:** чинить и включать классические 4 дорожки — см. `D-02`.
- **Отвергнуто:** реклама между партиями — см. `D-11`.

## DONE

Крупное и недавнее; старое — в [CHANGELOG.md](CHANGELOG.md).

- Серверный слой: подписанная отправка результата, общая таблица, DAU (`D-07`…`D-09`).
- Панель оператора с живой таблицей игроков.
- 14 языков с автоопределением и RTL для арабского.
- Личный кабинет: карьера, лиги, титулы, друзья, кланы, донат-витрина.
- Переизобретение DRIVE как Office Rage (`D-02`).
